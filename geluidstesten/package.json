{
  "name": "geluidstesten",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute geluidstesten --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}
